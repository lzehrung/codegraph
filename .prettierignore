tests/samples/**

# Generated by @napi-rs/cli during the native build; reformatting it only creates drift.
packages/codegraph-native/index.d.ts

# Generated by `npm run bench:fixtures`; `npm run bench:fixtures:check` compares the
# generator's own JSON.stringify output, so reformatting it breaks that gate.
docs/benchmarks/fixture-snapshot.example.json
.local/
