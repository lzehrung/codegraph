import { aliasValue, starValue, sourceNamespace } from "./reexport-barrel";

export const useAlias = aliasValue;
export const useStar = starValue;
export const useNamespace = sourceNamespace.namespacedValue;
